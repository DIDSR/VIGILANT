[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "VIGILANT"
version = "1.0.0"
dependencies=["pandas"]

[project.optional-dependencies]
docs = ["sphinx"]

[tools.setuptools.packages.find]
where = ["src"]