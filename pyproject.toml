[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "VIGILANT"
version = "1.0.0"
dependencies=["pandas"]

[tools.setuptools.packages.find]
where = ["src"]